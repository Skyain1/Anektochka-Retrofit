<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Anektochka" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
     <item name="colorPrimary">@color/brown_text</item>
        <item name="colorOnSurface">@color/background</item>
        <item name="android:statusBarColor">?attr/colorOnSurface</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Anektochka" parent="Base.Theme.Anektochka" />
</resources>